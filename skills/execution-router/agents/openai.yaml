interface:
  display_name: "Execution Router"
  short_description: "Idempotent state-aware routing from sealed plan or checkpointed review"
  default_prompt: "Use $execution-router from the primary repo root. Resolve the exact stage worktree from Git/provenance without guessing or git-switching the primary checkout, then dispatch the correct SINGLE or MULTI execution path inside that stage worktree."
