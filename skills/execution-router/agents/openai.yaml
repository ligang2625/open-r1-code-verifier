interface:
  display_name: "Execution Router"
  short_description: "State-aware execution routing with runtime local/web backend"
  default_prompt: "Use $execution-router from the primary repo root. Derive exact stage/provenance first, then honor backend=local|web. Local dispatches executor-ex/executor; web continues in the current GPT + CodexPro context with executor-web, serializing source MULTI without changing sealed routing."
