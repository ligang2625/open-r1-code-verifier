interface:
  display_name: "Reviewer Ex"
  short_description: "Independent provenance-bound review + repair routing; local lifecycle checkpoints/finalizes"
  default_prompt: "Use $reviewer-ex to review the latest completed execution for one explicit stage_id, append review_record and repair_routing, and stop for local $stage-lifecycle checkpoint_review."
