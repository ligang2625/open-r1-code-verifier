interface:
  display_name: "Reviewer Ex"
  short_description: "Backend-agnostic provenance review + repair routing"
  default_prompt: "Use $reviewer-ex for one explicit stage_id. Open the exact stage worktree, review the latest completed execution with the same standard regardless of local_codex/web_codexpro or effective mode, append review_record and repair_routing, then stop for shared $stage-lifecycle checkpoint_review."
