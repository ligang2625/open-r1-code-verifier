interface:
  display_name: "Reviewer Ex"
  short_description: "Backend-agnostic provenance review + repair routing"
  default_prompt: "Use $reviewer-ex only in a fresh Web GPT conversation that did not implement the latest execution. Resolve stage_id explicitly when provided; otherwise auto-select only when exactly one active stage worktree exists. Open that exact worktree and rebuild state from Git/sealed artifacts. For terminal development, independently verify the WP0-WP8 Development Completion Inventory plus the full closeout suite; DEV-CLOSEOUT may legitimately have result_code_commit equal plan_commit with no business diff. Validation must prove real 24GB-class evidence and persistent artifacts outside the worktree. Append review_record and repair_routing, then stop for shared $stage-lifecycle checkpoint_review."
