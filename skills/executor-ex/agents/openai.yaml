interface:
  display_name: "Executor Ex"
  short_description: "Local SINGLE execution for execution-router backend=local"
  default_prompt: "Use $executor-ex only for execution-router backend=local with source_mode=single. Preserve exact routed provenance/scope. A tracked-clean partial execution blocked only by an external environment issue may append a resumable execution_checkpoint instead of forcing retire; explicit router resume continues only remaining_scope. On completion commit code first, then append the shared v2 execution_record with local_codex/single audit metadata."
