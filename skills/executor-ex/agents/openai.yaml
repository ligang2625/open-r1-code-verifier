interface:
  display_name: "Executor Ex"
  short_description: "Execute a WP plan: code, tests, verification"
  default_prompt: "Use $executor-ex to execute the WP plan at ai-work/planner/WP1-plan.md and report results."
