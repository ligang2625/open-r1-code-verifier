interface:
  display_name: "Legacy Codex Planner"
  short_description: "Legacy full-local planner that creates the stage worktree"
  default_prompt: "Use $planner only for the legacy full-local planner → executor → reviewer workflow. It may create the stage branch/worktree. For v2 Web/CodexPro workflow use $planner-ex, then local $stage-lifecycle bootstrap_plan instead."
