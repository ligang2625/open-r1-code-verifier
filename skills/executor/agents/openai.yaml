interface:
  display_name: "Codex Executor"
  short_description: "Local Sol/Luna MULTI execution for backend=local"
  default_prompt: "Use $executor only for routed backend=local with source_mode=multi. Require at least two real independent lanes, isolate worker writes, and append the shared v2 execution_record with local_codex/multi audit metadata after coordinator verification and commits."
