interface:
  display_name: "Codex Executor"
  short_description: "Sol/Luna MULTI implementation or exact routed repair with execution provenance"
  default_prompt: "Use $executor for the router-provided MULTI task_kind, require at least two real independent lanes, isolate worker writes, and append a structured execution_record after coordinator verification and commits."
