interface:
  display_name: "Executor Web"
  short_description: "Web GPT + CodexPro routed execution with serialized MULTI"
  default_prompt: "Use $executor-web only after $execution-router selected backend=web. Open the exact stage worktree, preserve sealed source routing, execute SINGLE directly or MULTI as serialized_multi, append exactly one v2 execution_record without rewriting history, commit the report, then stop. reviewer-ex must run in a fresh Web conversation; do not self-review or finalize."
