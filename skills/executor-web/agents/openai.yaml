interface:
  display_name: "Executor Web"
  short_description: "Web GPT + CodexPro routed execution with serialized MULTI"
  default_prompt: "Use $executor-web after $execution-router selects backend=web. Open the correct stage worktree. Treat sealed routing as the default, but user instructions override implementation/scope/order/routing, and real dependencies may change the effective mode or lane layout. For partial execution, use checkpoints when helpful but also infer completed/remaining scope from Git, diffs, tests, reports, and user intent; do not require an exact current-head checkpoint and do not retire unless clearly unrecoverable or explicitly abandoned. Keep stage identity, no duplicate completed execution, .ai-bridge isolation, formal evidence/hardware boundaries, and actual operator handoff/script/evidence hashes strict. Record effective routing/continuation judgment, append history, then stop; reviewer-ex runs in a fresh Web conversation."
