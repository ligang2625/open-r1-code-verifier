run_name: refresh-hidden-grpo
reward_mode: hidden
dataset_path: outputs/wp9-calibration/training/hidden_grpo.jsonl
piston_config: configs/execution/piston-local.yaml
num_generations: 8
max_prompt_length: 1024
max_completion_length: 512
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 0.000005
num_train_epochs: 1.0
max_steps: 300
warmup_ratio: 0.05
lr_scheduler_type: cosine
temperature: 0.8
top_p: 0.95
beta: 0.01
bf16: true
fp16: false
gradient_checkpointing: true
lora_r: 16
lora_alpha: 32
lora_dropout: 0.05
logging_steps: 1
save_steps: 50
eval_steps: 50
seed: 42
min_cuda_memory_gb: 20.0
